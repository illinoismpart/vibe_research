# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual environments
.venv/
venv/
ENV/
env/

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# Data (raw documents and parsed output — not versioned)
data/raw/
data/parsed/

# Quarantine — documents flagged by PII/PHI sniffer pending human review.
# These are intentionally NOT versioned. The .gitkeep preserves the directory.
data/quarantine/
!data/quarantine/.gitkeep

# Environment and secrets
.env
.env.local
*.pem

# manifest.lock, manifest.json.sig, and audit_log.csv are intentionally
# TRACKED in git — they are the integrity and audit anchors.
# Do NOT add them here.
# data/manifest.lock
# data/manifest.json.sig
# data/audit_log.csv

# OS
.DS_Store
Thumbs.db
